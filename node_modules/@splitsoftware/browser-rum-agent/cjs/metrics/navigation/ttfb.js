"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTTFB = exports.calculateTTFB = void 0;
/**
 * Time to First Byte
 * We will check the difference from the point in time where the request for the document was issued and
 * when the response started arriving (the first byte!).
 */
var simpleMetricFactory_1 = require("./simpleMetricFactory");
// exported just for testing, rollup should avoid creating anything for it if we do not import it directly from the app.
function calculateTTFB(navEntry) {
    // Works for both levels of the Navigation Timing API.
    if (navEntry.requestStart && navEntry.responseStart) {
        return navEntry.responseStart - navEntry.requestStart;
    }
    return false;
}
exports.calculateTTFB = calculateTTFB;
exports.getTTFB = (0, simpleMetricFactory_1.simpleMetricFactory)('time.to.first.byte', calculateTTFB);
